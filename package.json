{
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "lint": "eslint --ignore-path .gitignore .",
    "test": "lerna run test"
  },
  "devDependencies": {
    "eslint": "^3.16.1",
    "eslint-config-fbjs-opensource": "^1.0.0",
    "lerna": "2.0.0-beta.38"
  }
}
